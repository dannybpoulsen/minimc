#ifndef __SHIFTS__
#define __SHIFTS__

namespace MiniMC {
  namespace Host {
    template <typename T>
    T ashr(T l, T r);

    template <typename T>
    T lshr(T l, T r);

  } // namespace Support
} // namespace MiniMC

#endif
